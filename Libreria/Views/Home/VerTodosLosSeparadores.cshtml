@model List<Libreria.Models.Separador>

@{
    Layout = null;

    // Asegúrate de que Model sea una lista de separadores
    var separadoresOrdenados = Model.OrderByDescending(s => s.SeparadorID).ToList();
}
<!-- Estilos para el efecto hover del ícono de carrito -->
<style>
    .btn-cart-icon {
        font-size: 1.5rem;
        color: blue;
        transition: color 0.3s ease, box-shadow 0.3s ease;
    }

        .btn-cart-icon:hover {
            color: #00BFFF; /* Azul celeste */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3); /* Sombra */
        }

</style>

@Html.Partial("_navbar")

<div class="container content mt-2">
    <h5 class="text-dark fw-bold text-center" style="font-size: 1.50rem;">TODOS LOS SEPARADORES</h5>


    @for (int i = 0; i < separadoresOrdenados.Count; i += 3)
    {
        <div class="row">
            @for (int j = i; j < i + 3 && j < separadoresOrdenados.Count; j++)
            {
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card d-flex flex-row h-100 position-relative">
                        <a asp-action="DetallesSeparador" asp-controller="Home" asp-route-id="@separadoresOrdenados[j].SeparadorID">
                            @if (separadoresOrdenados[j].Foto != null && separadoresOrdenados[j].Foto.Length > 0)
                            {
                                <img src="data:image/jpeg;base64,@Convert.ToBase64String(separadoresOrdenados[j].Foto)" alt="@separadoresOrdenados[j].Nombre" class="img-fluid" style="width: 120px; height: 180px; object-fit: cover;" />
                            }
                            else
                            {
                                <img src="~/img/no-foto.png" alt="No hay imagen disponible" class="img-fluid" style="width: 120px; height: 180px; object-fit: cover;" />
                            }
                        </a>

                        <div class="card-body d-flex flex-column">
                            <h6 class="card-title mb-2" style="overflow: hidden; text-overflow: ellipsis; white-space: normal; margin: 0;">@separadoresOrdenados[j].Nombre</h6>
                            <p class="card-text text-primary small mb-1">RD @separadoresOrdenados[j].Precio.ToString("C")</p>

                            <!-- Línea azul debajo del precio -->
                            <hr class="my-2" style="border-top: 2px solid blue;" />

                            <!-- Formulario para agregar al carrito -->
                            <form asp-action="AgregarSeparador" asp-controller="Carrito" method="post" class="d-inline">
                                <input type="hidden" name="separadorId" value="@separadoresOrdenados[j].SeparadorID" />
                                <button type="submit" class="btn p-0" title="Agregar al carrito">
                                    <i class="bi bi-cart-plus btn-cart-icon"></i>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<br />
<br />

<!-- Bootstrap Icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

<footer style="background-color: #002B70;" class="text-white text-center py-4">
    <div class="container">
        <p>&copy; 2024 Librería El Maestro. Todos los derechos reservados.</p>
        <p>
            <i class="bi bi-telephone-fill"></i>
            <a href="tel:+18096817079" class="text-white text-decoration-none">809-681-7079</a>
        </p>
        <p>
            <i class="bi bi-envelope-fill"></i>
            <a href="mailto:libmaestro@hotmail.com" class="text-white text-decoration-none">libmaestro@hotmail.com</a>
        </p>
        <p>
            <i class="bi bi-geo-alt-fill"></i>
            Dirección: C/Padre Castellanos No. 91, Ensanche Espaillat, Santo Domingo, República Dominicana.
        </p>
        <hr class="border border-light my-4">
        <p>Síguenos en nuestras redes sociales:</p>
        <div>
            <a href="https://facebook.com" class="text-white me-3">
                <i class="bi bi-facebook" style="font-size: 2rem;"></i>
            </a>
            <a href="https://instagram.com" class="text-white">
                <i class="bi bi-instagram" style="font-size: 2rem;"></i>
            </a>
        </div>
    </div>
</footer>
